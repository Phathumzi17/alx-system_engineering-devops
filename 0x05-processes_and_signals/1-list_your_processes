#!/usr/bin/env bash
# This script displays list of currently running processes
# All processes must show, for all users, including those which might not have a TTY
# Display a user-oriented format
# Show process hierarchy

ps -a -x -u -f
